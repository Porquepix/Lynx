{
    "start": {
        "init": "i = 0; j = 1;", // Code triggers after the context is loaded (Optional)
        "text": "Hello", // Text to display (If start with 't$', the translation system will be used)
        "answer": { // Option about the user answer
            "type": "integer", // Type of the answer : answer, double, integer, string, void
            "range": { // If i is integer or double, min <= i < max. If s is string, min <= s.length < max. If answer, nothing. If void, error. (Optional)
                "min": 0,
                "max": 2
            },
            "after": "i = _answer;" // Code triggers after the user answer and before the next context
        },
        "answers":[ // The answers poposed for the user (Only when answer type is 'answer')
            "Ok",
            "Test"
        ],
        "next": [
            {
                "condition": "i == 0",
                "context": "iE0"
            },
            {
                "condition": "j == 1",
                "context": "jE1"
            },
            {
                "context": "default"
            } 
        ]
    },

    ""
}



text --required
text to display
if the text starts with 't$', the string which follows the $ is a translate id

type --required
type of the context
    -> message: only a message
    -> answer: message with answers input
    -> integer: message with integer input
    -> string: message with string input
